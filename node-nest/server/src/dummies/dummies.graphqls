type DummyEntity {
  id: String!
  random1: String
  random2: Int
}

type Ring {
  name: String!
}

type Query {
  dummies(index: Int): [DummyEntity!]!
  getDummyById(id: String!): DummyEntity
  getSecret(bla: String): Ring
}

type Mutation {
  createDummy(id: String!): DummyEntity!
}
